<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Error - ShortVideo Admin</title>
    
    <!-- CSS -->
    <link href="https://cdnjs.cloudflare.com/ajax/libs/font-awesome/6.4.0/css/all.min.css" rel="stylesheet">
    
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }

        :root {
            --primary-color: #6366f1;
            --primary-dark: #4f46e5;
            --danger-color: #ef4444;
            --warning-color: #f59e0b;
            --success-color: #22c55e;
            --info-color: #3b82f6;
            
            --dark-bg: #0f172a;
            --dark-surface: #1e293b;
            --dark-card: #334155;
            --dark-text: #f8fafc;
            --dark-text-secondary: #cbd5e1;
            
            --border-radius: 12px;
            --shadow: 0 4px 6px -1px rgba(0, 0, 0, 0.1);
            --shadow-lg: 0 10px 15px -3px rgba(0, 0, 0, 0.1);
            --transition: all 0.3s ease;
        }

        body {
            font-family: 'Inter', -apple-system, BlinkMacSystemFont, 'Segoe UI', Roboto, sans-serif;
            background: linear-gradient(135deg, var(--dark-bg) 0%, #1a202c 100%);
            color: var(--dark-text);
            min-height: 100vh;
            display: flex;
            align-items: center;
            justify-content: center;
            padding: 20px;
        }

        .error-container {
            background: var(--dark-surface);
            border-radius: var(--border-radius);
            padding: 60px 40px;
            text-align: center;
            max-width: 600px;
            width: 100%;
            border: 1px solid rgba(255, 255, 255, 0.1);
            box-shadow: var(--shadow-lg);
        }

        .error-icon {
            font-size: 5rem;
            color: var(--danger-color);
            margin-bottom: 24px;
            animation: pulse 2s infinite;
        }

        @keyframes pulse {
            0%, 100% { transform: scale(1); }
            50% { transform: scale(1.05); }
        }

        .error-code {
            font-size: 8rem;
            font-weight: 900;
            color: var(--primary-color);
            margin-bottom: 16px;
            text-shadow: 0 4px 8px rgba(99, 102, 241, 0.3);
        }

        .error-title {
            font-size: 2rem;
            font-weight: 700;
            color: var(--dark-text);
            margin-bottom: 16px;
        }

        .error-message {
            font-size: 1.125rem;
            color: var(--dark-text-secondary);
            margin-bottom: 32px;
            line-height: 1.6;
        }

        .error-details {
            background: var(--dark-card);
            border-radius: var(--border-radius);
            padding: 20px;
            margin-bottom: 32px;
            text-align: left;
        }

        .error-details h4 {
            color: var(--dark-text);
            margin-bottom: 12px;
            font-size: 1rem;
            font-weight: 600;
        }

        .error-details pre {
            color: var(--danger-color);
            font-size: 0.875rem;
            white-space: pre-wrap;
            word-break: break-word;
            max-height: 200px;
            overflow-y: auto;
        }

        .error-actions {
            display: flex;
            gap: 16px;
            justify-content: center;
            flex-wrap: wrap;
            margin-bottom: 32px;
        }

        .btn {
            display: inline-flex;
            align-items: center;
            gap: 8px;
            padding: 12px 24px;
            border: none;
            border-radius: var(--border-radius);
            font-weight: 600;
            text-decoration: none;
            cursor: pointer;
            transition: var(--transition);
            font-size: 0.875rem;
        }

        .btn-primary {
            background: var(--primary-color);
            color: white;
        }

        .btn-primary:hover {
            background: var(--primary-dark);
            transform: translateY(-2px);
            box-shadow: var(--shadow);
        }

        .btn-secondary {
            background: var(--dark-card);
            color: var(--dark-text);
            border: 1px solid rgba(255, 255, 255, 0.2);
        }

        .btn-secondary:hover {
            background: rgba(255, 255, 255, 0.1);
            transform: translateY(-2px);
        }

        .btn-danger {
            background: var(--danger-color);
            color: white;
        }

        .btn-danger:hover {
            background: #dc2626;
            transform: translateY(-2px);
        }

        .error-suggestions {
            background: rgba(59, 130, 246, 0.1);
            border: 1px solid rgba(59, 130, 246, 0.3);
            border-radius: var(--border-radius);
            padding: 24px;
            margin-bottom: 24px;
        }

        .error-suggestions h4 {
            color: var(--info-color);
            margin-bottom: 16px;
            font-size: 1.125rem;
            font-weight: 600;
            display: flex;
            align-items: center;
            gap: 8px;
        }

        .error-suggestions ul {
            list-style: none;
            text-align: left;
        }

        .error-suggestions li {
            padding: 8px 0;
            padding-left: 24px;
            position: relative;
            color: var(--dark-text-secondary);
            line-height: 1.5;
        }

        .error-suggestions li::before {
            content: "💡";
            position: absolute;
            left: 0;
            top: 8px;
        }

        .error-footer {
            text-align: center;
            padding-top: 24px;
            border-top: 1px solid rgba(255, 255, 255, 0.1);
            color: var(--dark-text-secondary);
            font-size: 0.875rem;
        }

        .error-footer a {
            color: var(--primary-color);
            text-decoration: none;
            font-weight: 500;
        }

        .error-footer a:hover {
            text-decoration: underline;
        }

        /* Responsive */
        @media (max-width: 768px) {
            .error-container {
                padding: 40px 20px;
                margin: 20px;
            }

            .error-code {
                font-size: 6rem;
            }

            .error-title {
                font-size: 1.5rem;
            }

            .error-actions {
                flex-direction: column;
                align-items: center;
            }

            .btn {
                width: 100%;
                max-width: 300px;
                justify-content: center;
            }
        }

        /* Animation */
        .error-container {
            animation: slideUp 0.6s ease-out;
        }

        @keyframes slideUp {
            from {
                opacity: 0;
                transform: translateY(30px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>
    <div class="error-container">
        <% if (typeof error !== 'undefined' && error.status) { %>
            <div class="error-code"><%= error.status %></div>
        <% } else { %>
            <div class="error-icon">
                <i class="fas fa-exclamation-triangle"></i>
            </div>
        <% } %>

        <h1 class="error-title">
            <% if (typeof error !== 'undefined' && error.status === 404) { %>
                Halaman Tidak Ditemukan
            <% } else if (typeof error !== 'undefined' && error.status === 403) { %>
                Akses Ditolak
            <% } else if (typeof error !== 'undefined' && error.status >= 500) { %>
                Kesalahan Server
            <% } else { %>
                Terjadi Kesalahan
            <% } %>
        </h1>

        <p class="error-message">
            <%= typeof message !== 'undefined' ? message : 'Terjadi kesalahan yang tidak terduga di sistem admin.' %>
        </p>

        <% if (typeof error !== 'undefined' && error.stack && process.env.NODE_ENV === 'development') { %>
        <div class="error-details">
            <h4><i class="fas fa-bug"></i> Error Details (Development Mode)</h4>
            <pre><%= error.stack %></pre>
        </div>
        <% } %>

        <div class="error-suggestions">
            <h4>
                <i class="fas fa-lightbulb"></i>
                Saran Penyelesaian
            </h4>
            <ul>
                <% if (typeof error !== 'undefined' && error.status === 404) { %>
                    <li>Periksa URL yang Anda masukkan</li>
                    <li>Gunakan menu navigasi untuk mencari halaman yang diinginkan</li>
                    <li>Kembali ke dashboard admin</li>
                <% } else if (typeof error !== 'undefined' && error.status === 403) { %>
                    <li>Pastikan Anda memiliki permission yang sesuai</li>
                    <li>Hubungi administrator sistem untuk akses</li>
                    <li>Login ulang dengan akun yang memiliki akses</li>
                <% } else if (typeof error !== 'undefined' && error.status >= 500) { %>
                    <li>Refresh halaman dan coba lagi</li>
                    <li>Periksa koneksi database</li>
                    <li>Cek log server untuk detail error</li>
                    <li>Hubungi tim development jika masalah berlanjut</li>
                <% } else { %>
                    <li>Refresh halaman dan coba lagi</li>
                    <li>Periksa koneksi internet Anda</li>
                    <li>Logout dan login kembali</li>
                    <li>Hubungi administrator sistem jika masalah berlanjut</li>
                <% } %>
            </ul>
        </div>

        <div class="error-actions">
            <a href="/admin/dashboard" class="btn btn-primary">
                <i class="fas fa-tachometer-alt"></i>
                Kembali ke Dashboard
            </a>
            
            <button onclick="history.back()" class="btn btn-secondary">
                <i class="fas fa-arrow-left"></i>
                Halaman Sebelumnya
            </button>
            
            <button onclick="location.reload()" class="btn btn-secondary">
                <i class="fas fa-redo"></i>
                Refresh Halaman
            </button>

            <% if (typeof error !== 'undefined' && error.status >= 500) { %>
            <a href="/admin/help" class="btn btn-danger">
                <i class="fas fa-life-ring"></i>
                Bantuan
            </a>
            <% } %>
        </div>

        <div class="error-footer">
            <p>
                Jika masalah ini terus berlanjut, silakan hubungi 
                <a href="mailto:support@shortvideo.com">Tim Support</a> atau 
                <a href="/admin/help">lihat dokumentasi bantuan</a>.
            </p>
            <p style="margin-top: 8px;">
                <strong>Error ID:</strong> <%= Date.now().toString(36) %>-<%= Math.random().toString(36).substr(2, 9) %>
            </p>
        </div>
    </div>

    <script>
        // Auto-refresh for server errors after 30 seconds
        <% if (typeof error !== 'undefined' && error.status >= 500) { %>
        setTimeout(() => {
            if (confirm('Ingin mencoba memuat ulang halaman secara otomatis?')) {
                location.reload();
            }
        }, 30000);
        <% } %>

        // Keyboard shortcuts
        document.addEventListener('keydown', (e) => {
            if (e.key === 'Escape') {
                history.back();
            } else if (e.key === 'r' && (e.ctrlKey || e.metaKey)) {
                e.preventDefault();
                location.reload();
            } else if (e.key === 'h' && (e.ctrlKey || e.metaKey)) {
                e.preventDefault();
                window.location.href = '/admin/dashboard';
            }
        });

        // Log error for analytics
        if (typeof error !== 'undefined' && window.console) {
            console.group('🚨 Admin Error Details');
            console.error('Status:', '<%= typeof error !== "undefined" && error.status ? error.status : "Unknown" %>');
            console.error('Message:', '<%= typeof message !== "undefined" ? message : "Unknown error" %>');
            console.error('URL:', window.location.href);
            console.error('User Agent:', navigator.userAgent);
            console.error('Timestamp:', new Date().toISOString());
            console.groupEnd();
        }
    </script>
</body>
</html>